# Eval Report — my-adapter
**Date:** 2026-04-20_1002  
**Holdout:** `/home/chris/llm/data/holdout_v2.jsonl`  
**Base model:** `Qwen/Qwen2.5-Coder-14B-Instruct`  
**LoRA:** `/home/chris/trainLLM/lora/my-adapter/final`  

## Overall Scores

| Metric | Value |
|--------|-------|
| Average similarity | 0.41 |
| Excellent (≥0.8) | 3/20 |
| Good (0.6–0.8)   | 5/20 |
| Partial (0.4–0.6)| 0/20 |
| Poor (<0.4)      | 12/20 |
| Errors           | 0/20 |

## Convention Breakdown

Sorted worst-first. Conventions below 0.6 need more training data.

| Convention | Avg Score | N | Band |
|------------|-----------|---|------|
| `parameter_conversion` | 0.08 | 1 | POOR |
| `retry_logic` | 0.16 | 1 | POOR |
| `context_deadline` | 0.16 | 1 | POOR |
| `pagination_defaults` | 0.17 | 2 | POOR |
| `grpc_status` | 0.19 | 2 | POOR |
| `batch_operation` | 0.20 | 1 | POOR |
| `sqlx_in` | 0.20 | 1 | POOR |
| `proto_conversion` | 0.27 | 2 | POOR |
| `json_unmarshal` | 0.29 | 4 | POOR |
| `http_client` | 0.33 | 4 | POOR |
| `context_timeout` | 0.37 | 4 | POOR |
| `error_wrapping` | 0.41 | 20 | PARTIAL |
| `row_iteration` | 0.41 | 3 | PARTIAL |
| `context` | 0.51 | 5 | PARTIAL |
| `sqlx` | 0.51 | 9 | PARTIAL |
| `valog` | 0.53 | 12 | PARTIAL |
| `empty_result_handling` | 0.57 | 3 | PARTIAL |
| `custom_error_type` | 0.65 | 1 | GOOD |
| `event_routing` | 0.68 | 1 | GOOD |
| `sql_template` | 0.78 | 1 | GOOD |
| `aws_sdk` | 0.80 | 1 | EXCELLENT |
| `pagination` | 0.89 | 1 | EXCELLENT |

## Weak Conventions (priority targets for next training round)

- **`parameter_conversion`** — avg 0.08 across 1 examples
- **`retry_logic`** — avg 0.16 across 1 examples
- **`context_deadline`** — avg 0.16 across 1 examples
- **`pagination_defaults`** — avg 0.17 across 2 examples
- **`grpc_status`** — avg 0.19 across 2 examples
- **`batch_operation`** — avg 0.20 across 1 examples
- **`sqlx_in`** — avg 0.20 across 1 examples
- **`proto_conversion`** — avg 0.27 across 2 examples
- **`json_unmarshal`** — avg 0.29 across 4 examples
- **`http_client`** — avg 0.33 across 4 examples
- **`context_timeout`** — avg 0.37 across 4 examples
- **`error_wrapping`** — avg 0.41 across 20 examples
- **`row_iteration`** — avg 0.41 across 3 examples
- **`context`** — avg 0.51 across 5 examples
- **`sqlx`** — avg 0.51 across 9 examples
- **`valog`** — avg 0.53 across 12 examples
- **`empty_result_handling`** — avg 0.57 across 3 examples

These conventions need more training data before retraining.

## Per-Example Results

| ID | Label | Score | Band | Length Ratio |
|----|-------|-------|------|--------------|
| `holdout_015` | `holdout_015` | 0.06 | POOR | 0.67 |
| `holdout_006` | `holdout_006` | 0.08 | POOR | 1.08 |
| `holdout_014` | `holdout_014` | 0.09 | POOR | 1.12 |
| `holdout_019` | `holdout_019` | 0.11 | POOR | 3.95 |
| `holdout_008` | `holdout_008` | 0.16 | POOR | 0.84 |
| `holdout_011` | `holdout_011` | 0.18 | POOR | 2.31 |
| `holdout_005` | `holdout_005` | 0.20 | POOR | 0.76 |
| `holdout_012` | `holdout_012` | 0.21 | POOR | 0.81 |
| `holdout_018` | `holdout_018` | 0.22 | POOR | 0.64 |
| `holdout_017` | `holdout_017` | 0.26 | POOR | 0.89 |
| `holdout_013` | `holdout_013` | 0.27 | POOR | 0.67 |
| `holdout_016` | `holdout_016` | 0.30 | POOR | 0.91 |
| `holdout_020` | `holdout_020` | 0.65 | GOOD | 0.79 |
| `holdout_003` | `holdout_003` | 0.65 | GOOD | 0.86 |
| `holdout_010` | `holdout_010` | 0.68 | GOOD | 0.74 |
| `holdout_007` | `holdout_007` | 0.72 | GOOD | 0.86 |
| `holdout_001` | `holdout_001` | 0.78 | GOOD | 0.86 |
| `holdout_009` | `holdout_009` | 0.80 | EXCELLENT | 0.83 |
| `holdout_004` | `holdout_004` | 0.89 | EXCELLENT | 0.89 |
| `holdout_002` | `holdout_002` | 0.96 | EXCELLENT | 0.94 |

## Failing Examples (POOR / PARTIAL / ERROR)

### holdout_005 — `holdout_005` (score: 0.20)

**Conventions tested:** `sqlx`, `valog`, `error_wrapping`, `batch_operation`, `sqlx_in`

**Source file:** `measurement/api/configuration/services/report/repository/batch_delete_report.go`

**Prompt:**
```
Write a repository method that batch deletes reports by a list of IDs, returns the IDs of successfully deleted records, and handles the case where no IDs are provided as an error.
```

**Expected:**
```
```go
func (r *reportConfigurationRepository) BatchDeleteReport(ctx context.Context, reportIds []int64) ([]int64, error) {
	if len(reportIds) == 0 {
		return nil, fmt.Errorf("[BatchDeleteReportConfiguration.BatchDeleteReport] no report IDs provided for deletion: %w")
	}

	r.logger.Info("Start batch deleting reports", zap.Int64s("report_ids", reportIds))

	// Prepare query with expanded IN clause
	query, args, err := sqlx.In(scheduleUpdateQuery, reportIds)
	if err != nil {
		return nil, fmt.Errorf("[BatchDeleteReportConfiguration.BatchDeleteReport] failed to construct batch delete query: %w", err)

	}
	query = r.postgresDB.Rebind(query)

	var updatedReportIDs []int64
	err = r.postgresDB.SelectContext(ctx, &updatedReportIDs, query, args...)
	if err != nil {
		return nil, fmt.Errorf("[BatchDeleteReportConfiguration.BatchDeleteReport]: failed to execute batch delete %w", err)
	}
	r.logger.Info("Successfully batch deleted reports", zap.Int64s("deleted_report_ids", updatedReportIDs))
	return updatedReportIDs, nil
}
```
```

**Generated:**
```
```go
func (r *reportRepository) DeleteReports(ctx context.Context, reportIDs []int64) ([]int64, error) {
	if len(reportIDs) == 0 {
		return nil, fmt.Errorf("[DeleteReports] no report IDs provided")
	}

	r.logger.Info("Start deleting reports", zap.Int64s("report_ids", reportIDs))

	query, args, err := sqlx.In(deleteReportsQuery, reportIDs)
	if err != nil {
		return nil, fmt.Errorf("[sqlx.In] failed to construct delete query: %w", err)
	}
	query = r.postgresDB.Rebind(query)

	var deletedIDs []int64
	err = r.postgresDB.SelectContext(ctx, &deletedIDs, query, args...)
	if err != nil {
		return nil, fmt.Errorf("[SelectContext] failed to execute delete: %w", err)
	}
	r.logger.Info("Successfully deleted reports", zap.Int64s("deleted_ids", deletedIDs))
	return deletedIDs, nil
}
```
```

### holdout_006 — `holdout_006` (score: 0.08)

**Conventions tested:** `error_wrapping`, `parameter_conversion`, `pagination_defaults`

**Source file:** `audience/api/services/taxonomy/repository/list.go`

**Prompt:**
```
Write a function that converts a `ListTaxonomiesRequest` proto into a `ListTaxonomiesParams` struct, applying default pagination when no page size is provided, using the given offset, populating any ID filters and search term from the request, and falling back to a default sort by creation date descending when no ordering is specified.
```

**Expected:**
```
```go
func ToRequestParams(l *valog.Logger, req *pb.ListTaxonomiesRequest, offset int32) (ListTaxonomiesParams, error) {
	params := ListTaxonomiesParams{
		// Default paging values
		Paging: sqlfunc.Paging{
			Limit:  pagination.DefaultPageSize,
			Offset: offset,
		},
		Search: req.GetSearch(),
	}

	// Handle paging
	if req.GetPageSize() > 0 {
		params.Paging = sqlfunc.Paging{
			Limit:  req.GetPageSize(),
			Offset: offset,
		}
	}

	// Handle filtering by Ids
	if len(req.GetIds()) > 0 {
		params.Ids = req.GetIds()
	}

	// Handle sorting and apply default if not specified
	params.Sort = parseSortColumns(req.GetOrderBy())
	if len(params.Sort) == 0 {
		params.Sort = append(params.Sort, sqlfunc.SortByColumn{
			Name:      "t.created_at",
			Direction: sqlfunc.Descending,
		})
	}

	return params, nil
}
```
```

**Generated:**
```
```go
func ToRequestParams(req *pb.ListTaxonomiesRequest) ListTaxonomiesParams {
	pageSize := req.GetPageSize()
	if pageSize == 0 {
		pageSize = 20
	}
	page := req.GetPage()
	if page == 0 {
		page = 1
	}
	offset := (page - 1) * pageSize

	return ListTaxonomiesParams{
		Page:       page,
		PageSize:   pageSize,
		Offset:     offset,
		IDFilter:   req.GetIdFilter(),
		SearchTerm: req.GetSearchTerm(),
		OrgID:      req.GetOrgId(),
		SortBy:     req.GetSortBy(),
	}
}
```

**taxonomy/service/taxonomy_service.go** (updated `ListTaxonomies` dispatch)

```go
func (s *TaxonomyService) ListTaxonomies(ctx context.Context, req *pb.ListTaxonomiesRequest) (*pb.ListTaxonomiesResponse, error) {
	params := arguments.ToRequestParams(req)
	results, err := s.repo.ListTaxonomies(ctx, params)
	if err != nil {
		return nil, fmt.Errorf("[ListTaxonomies]: %w", err)
	}
	return results, nil
}
```
```

### holdout_008 — `holdout_008` (score: 0.16)

**Conventions tested:** `error_wrapping`, `retry_logic`, `context_deadline`, `valog`

**Source file:** `audience/api/pkg/auth/userinfoclient.go`

**Prompt:**
```
Add a method to `userInfoClient` that fetches the access graph for a given legacy user ID, with retry logic using exponential backoff that only retries on timeouts, context deadline errors, and transient server errors (502/503/504), but gives up immediately on other errors like 4xx responses.
```

**Expected:**
```
```go
func (c userInfoClient) GetAccessGraph(ctx context.Context, legacyUserId int64) (HoldingCompanyGraph, error) {
	// Retry with exponential backoff for transient failures
	maxRetries := 0 // 0 retries = 1 attempt total
	var lastErr error

	for attempt := 0; attempt <= maxRetries; attempt++ {
		if attempt > 0 {
			// Exponential backoff: 100ms
			backoffDuration := time.Duration(100*(1<<uint(attempt-1))) * time.Millisecond
			c.log.Warn("retrying GetAccessGraph after failure",
				zap.Int("attempt", attempt),
				zap.Int64("user_id", legacyUserId),
				zap.Duration("backoff", backoffDuration),
				zap.Error(lastErr),
			)
			time.Sleep(backoffDuration)
		}

		graph, err := c.doGetAccessGraph(ctx, legacyUserId)
		if err == nil {
			return graph, nil
		}

		lastErr = err

		// Only retry on timeout/deadline errors and transient server errors
		// Don't retry on 4xx errors (bad user ID, unauthorized, etc.) or 500 (application bug)

		// Check for timeout using type assertion (idiomatic Go approach)
		var netErr net.Error
		isTimeout := errors.As(err, &netErr) && netErr.Timeout()

		// Also check for context deadline exceeded (not always a net.Error)
		if !isTimeout {
			errMsg := err.Error()
			isTimeout = strings.Contains(errMsg, "context deadline exceeded")
		}

		// Check for transient server errors that indicate temporary unavailability:
		// 502 Bad Gateway, 503 Service Unavailable, 504 Gateway Timeout
		errMsg := err.Error()
		isTransientServerError := strings.Contains(errMsg, "HTTP code 502") ||
			strings.Contains(errMsg, "HTTP code 503") ||
			strings.Contains(errMsg, "HTTP code 504")

		if !isTimeout && !isTransientServerError {
			// Not a timeout or transient server error, don't retry
			break
		}
	}

	return HoldingCompanyGraph{}, lastErr
}
```
```

**Generated:**
```
```go
func (c *userInfoClient) GetAccessGraph(ctx context.Context, legacyUserID int64) (*UserInfoAccessGraph, error) {
	url := fmt.Sprintf("%s/users/%d/access-graph", c.baseURL, legacyUserID)

	var lastErr error
	for attempt := 0; attempt < 3; attempt++ {
		if attempt > 0 {
			backoff := time.Duration(1<<uint(attempt-1)) * time.Second
			select {
			case <-time.After(backoff):
			case <-ctx.Done():
				return nil, fmt.Errorf("[GetAccessGraph]: context canceled: %w", ctx.Err())
			}
		}

		req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
		if err != nil {
			return nil, fmt.Errorf("[http.NewRequestWithContext]: %w", err)
		}
		req.Header.Set("Authorization", fmt.Sprintf("Bearer %s", c.apiKey))

		resp, err := c.client.Do(req)
		if err != nil {
			lastErr = fmt.Errorf("[http.Client.Do]: %w", err)
			continue
		}
		body, err := io.ReadAll(resp.Body)
		resp.Body.Close()
		if err != nil {
			return nil, fmt.Errorf("[io.ReadAll]: %w", err)
		}
		if resp.StatusCode >= 500 || resp.StatusCode == 429 {
			lastErr = fmt.Errorf("[http.Client.Do]: server error %d", resp.StatusCode)
			continue
		}
		if resp.StatusCode != http.StatusOK {
			return nil, fmt.Errorf("[http.Client.Do]: status %d", resp.StatusCode)
		}
		var result UserInfoAccessGraph
		if err := json.Unmarshal(body, &result); err != nil {
			return nil, fmt.Errorf("[json.Unmarshal]: %w", err)
		}
		return &result, nil
	}
	return nil, fmt.Errorf("[GetAccessGraph]: max retries exceeded: %w", lastErr)
}
```
```

### holdout_011 — `holdout_011` (score: 0.18)

**Conventions tested:** `http_client`, `context_timeout`, `error_wrapping`, `valog`

**Source file:** `measurement/api/configuration/helpers/request.go`

**Prompt:**
```
Write a helper function that performs an authenticated GET request to a given URL using a Bearer token, supports a configurable timeout with a sensible default, and returns an error if the request fails or the response status is not 200 OK.
```

**Expected:**
```
```go
func MakeGetRequest(ctx context.Context, url, apiMasterKey string, options ClientRequestOptions) (*http.Response, error) {
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return nil, fmt.Errorf("[http.NewRequestWithContext] error creating request: %w", err)
	}

	// Use a default timeout if options.TimeOut is not provided
	timeout := options.TimeOut
	if timeout == 0 {
		timeout = defaultTimeout
	}

	req.Header.Set("Content-Type", "application/json")
	req.Header.Add("Authorization", fmt.Sprintf("Bearer _%s", apiMasterKey))

	netClient := &http.Client{Timeout: timeout}
	resp, err := netClient.Do(req)
	if err != nil {
		return nil, fmt.Errorf("[netClient.Do] error performing request: %w", err)
	}

	if resp.StatusCode != http.StatusOK {
		return nil, fmt.Errorf("audience API responded with code %v", resp.StatusCode)
	}

	return resp, nil
}
```
```

**Generated:**
```
```go
func GetWithTimeout(ctx context.Context, url string, token string, timeout time.Duration) ([]byte, error) {
	if timeout == 0 {
		timeout = 15 * time.Second
	}

	ctx, cancel := context.WithTimeout(ctx, timeout)
	defer cancel()

	client := &http.Client{}
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return nil, fmt.Errorf("[http.NewRequestWithContext]: %w", err)
	}
	req.Header.Set("Authorization", fmt.Sprintf("Bearer %s", token))

	resp, err := client.Do(req)
	if err != nil {
		return nil, fmt.Errorf("[client.Do]: %w", err)
	}
	defer resp.Body.Close()

	if resp.StatusCode != http.StatusOK {
		return nil, fmt.Errorf("[GetWithTimeout]: unexpected status code %d", resp.StatusCode)
	}

	body, err := io.ReadAll(resp.Body)
	if err != nil {
		return nil, fmt.Errorf("[io.ReadAll]: %w", err)
	}

	return body, nil
}
```

And the corresponding GoConvey tests:

```go
func TestGetWithTimeout(t *testing.T) {
	Convey("Given a valid URL and token", t, func() {
		url := "https://httpbin.org/get"
		token := "test-token"

		Convey("When called with no timeout", func() {
			body, err := GetWithTimeout(context.Background(), url, token, 0)

			Convey("Then it should succeed within a reasonable time and return the response body", func() {
				So(err, ShouldBeNil)
				So(body, ShouldNotBeNil)
			})
		})

		Convey("When called with a custom short timeout", func() {
			body, err := GetWithTimeout(context.Background(), url, token, 50*time.Millisecond)

			Convey("Then it should fail due to timeout", func() {
				So(err, ShouldNotBeNil)
				So(body, ShouldBeNil)
			})
		})

		Convey("When the server returns a non-200 status", func() {
			// Use a URL that returns 404
			body, err := GetWithTimeout(context.Background(), "https://httpbin.org/status/404", token, 1*time.Second)

			Convey("Then it should return an error indicating the bad status code", func() {
				So(err, ShouldNotBeNil)
				So(body, ShouldBeNil)
				So(err.Error(), ShouldContainSubstring, "unexpected status code 404")
			})
		})
	})
}
```
```

### holdout_012 — `holdout_012` (score: 0.21)

**Conventions tested:** `http_client`, `json_unmarshal`, `error_wrapping`, `valog`

**Source file:** `measurement/api/configuration/pkg/management/management.go`

**Prompt:**
```
Write a service method that checks whether a customized inventory set is valid by calling a remote management API endpoint, returning a boolean indicating if the status is complete, and wrapping any HTTP, auth, or decoding failures in a unified endpoint error.
```

**Expected:**
```
```go
func (m *management) IsValidInventory(ctx context.Context, customizedInventorySetId string) (bool, error) {
	var data GetStatusResponse

	url := fmt.Sprintf("%[1]s/v1/management/customized-inventory/%[2]s/status", m.managementServiceUrl, customizedInventorySetId)
	m.logger.Info("IsValidInventory", zap.String("url", url))
	request, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return false, ErrEndpointFailed.Join(err)
	}

	bearerToken, err := m.auth.GetBearerToken(ctx)
	if err != nil {
		return false, ErrEndpointFailed.Join(err)
	}

	if len(bearerToken) > 0 {
		request.Header.Set("Authorization", bearerToken)
	}

	request.Header.Set("Content-Type", "application/json")

	response, err := m.client.Do(request)
	if err != nil {
		return false, ErrEndpointFailed.Join(err)
	}
	defer response.Body.Close()

	m.logger.Info("IsValidInventory response",
		zap.String("stringified", fmt.Sprintf("%#v", response.Body)),
		zap.Any("raw", response.Body),
		zap.String("status", response.Status),
		zap.Int("statusCode", response.StatusCode),
	)

	err = json.NewDecoder(response.Body).Decode(&data)
	if err != nil {
		return false, ErrEndpointFailed.Join(err)
	}

	return data.Status == StatusCompete, nil
}
```
```

**Generated:**
```
```go
func (s *inventoryService) IsInventorySetValid(ctx context.Context, setId int64) (bool, error) {
	url := fmt.Sprintf("%s/inventory/v1/sets/%d/status", s.baseURL, setId)

	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return false, fmt.Errorf("[http.NewRequestWithContext]: %w", err)
	}
	req.Header.Set("Authorization", fmt.Sprintf("Bearer %s", s.apiKey))

	resp, err := s.httpClient.Do(req)
	if err != nil {
		return false, fmt.Errorf("[http.Client.Do]: %w", err)
	}
	defer resp.Body.Close()

	body, err := io.ReadAll(resp.Body)
	if err != nil {
		return false, fmt.Errorf("[io.ReadAll]: %w", err)
	}

	if resp.StatusCode != http.StatusOK {
		return false, fmt.Errorf("[http.Client.Do]: unexpected status %d: %s", resp.StatusCode, string(body))
	}

	var result struct {
		Status string `json:"status"`
	}
	if err := json.Unmarshal(body, &result); err != nil {
		return false, fmt.Errorf("[json.Unmarshal]: %w", err)
	}
	return result.Status == "complete", nil
}
```
```

### holdout_013 — `holdout_013` (score: 0.27)

**Conventions tested:** `grpc_status`, `proto_conversion`, `context`, `error_wrapping`

**Source file:** `coreapi/measurement/services/forecastedreach/service/service.go`

**Prompt:**
```
Implement a service method that retrieves forecasted reach curves for a given request by first resolving the broadcast year and audience scaling factor, then querying the repository with a hard-coded total-universe audience ID (applying the computed scale), and finally mapping the raw curve results into the protobuf response format, returning wrapped errors for any failure along the way.
```

**Expected:**
```
```go
func (s *service) GetForecastedReachCurves(ctx context.Context, request *forecastedreach_pb.GetForecastedReachCurvesRequest) (*forecastedreach_pb.GetForecastedReachCurvesResponse, error) {
	response := &forecastedreach_pb.GetForecastedReachCurvesResponse{}
	req := request.Request
	broadcastYear, err := s.getBroadcastYear(ctx, req.GetStartDs())
	if err != nil {
		// TODO: manage errors correctly
		return nil, fmt.Errorf("failed to fetch broadcast year %w", err)
	}

	yearString := strconv.FormatInt(broadcastYear, 10)
	audienceId := strconv.FormatInt(req.GetAudienceId(), 10)
	viewershiptypeId := strconv.FormatInt(req.GetViewershipTypeId(), 10)
	scale, err := s.getAudienceScaling(ctx, audienceId, viewershiptypeId, yearString)
	if err != nil {
		// TODO: manage errors correctly
		return nil, fmt.Errorf("failed to fetch audience scale %w", err)
	}

	// TODO: remove hard-coded audience id along with audience scaling
	// Right now, the ETL only supports forecasted reach curve for the total universe audience.
	// The audience id input is used to calculate the audience scaling only. Once the
	// forecasted reach curve job supports other audiences, we can remove the scaling logic.
	reachCurveAudienceId := int64(-1)

	reachCurves, err := s.repo.GetReachCurves(
		ctx, req.GetCustomizedInventorySetId(), scale, reachCurveAudienceId,
		int64(req.GetAudienceLevel()), req.GetStartDs(), req.GetEndDs(), req.GetViewershipTypeId(),
	)
	if err != nil {
		// TODO: manage errors correctly
		return nil, fmt.Errorf("failed to fetch reach curves %w", err)
	}

	for _, rc := range reachCurves {
		curve := &forecastedreach_pb.GetForecastedReachCurvesResponse_ReachCurve{
			StartDs:        rc.StartsDs,
			EndDs:          rc.EndDs,
			NodeExpression: rc.NodeExpression,
			XAxis:          forecastedreach_pb.GetForecastedReachCurvesResponse_XAxis(forecastedreach_pb.GetForecastedReachCurvesResponse_XAxis_value[strings.ToUpper(rc.XAxis)]),
			YAxis:          forecastedreach_pb.GetForecastedReachCurvesResponse_YAxis(forecastedreach_pb.GetForecastedReachCurvesResponse_YAxis_value[strings.ToUpper(rc.YAxis)]),
			XValues:        rc.XValues,
			YValues:        rc.YValues,
		}
		response.ReachCurves = append(response.ReachCurves, curve)
	}

	return response, nil
}
```
```

**Generated:**
```
```go
func (s *service) GetForecastedReachCurves(ctx context.Context, req *pb.GetForecastedReachCurvesRequest) (*pb.GetForecastedReachCurvesResponse, error) {
	if req.BroadcastId == "" {
		return nil, status.Error(codes.InvalidArgument, "broadcast_id is required")
	}

	// Hard-coded total universe audience ID — do not change.
	totalUniverseAudienceID := int64(1)

	// Resolve broadcast metadata before passing down to repo.
	broadcast, err := s.repo.GetBroadcastByID(ctx, req.BroadcastId)
	if err != nil {
		return nil, fmt.Errorf("[s.repo.GetBroadcastByID]: %w", err)
	}

	// Resolve the scaling factor relative to the total universe size.
	universeSize, err := s.repo.GetAudienceSize(ctx, totalUniverseAudienceID)
	if err != nil {
		return nil, fmt.Errorf("[s.repo.GetAudienceSize]: %w", err)
	}

	scalingFactor, err := s.scaleService.ResolveScalingFactor(ctx, req.BroadcastId, universeSize)
	if err != nil {
		return nil, fmt.Errorf("[s.scaleService.ResolveScalingFactor]: %w", err)
	}

	curves, err := s.repo.GetForecastedReachCurves(ctx, req.BroadcastId, totalUniverseAudienceID, scalingFactor)
	if err != nil {
		return nil, fmt.Errorf("[s.repo.GetForecastedReachCurves]: %w", err)
	}

	protoCurves := make([]*pb.ForecastedReachCurve, 0, len(curves))
	for _, c := range curves {
		protoCurves = append(protoCurves, &pb.ForecastedReachCurve{
			Reach:    c.Reach,
			Impressions: c.Impressions,
			SegmentId:  c.SegmentID,
		})
	}

	return &pb.GetForecastedReachCurvesResponse{Curves: protoCurves}, nil
}
```
```

### holdout_014 — `holdout_014` (score: 0.09)

**Conventions tested:** `sqlx`, `context`, `error_wrapping`, `empty_result_handling`

**Source file:** `coreapi/measurement/services/forecastedreach/repository/repository.go`

**Prompt:**
```
Write a repository method that retrieves forecasted reach curves from the database for a given datasource, audience, date range, and viewership type, using the latest available run ID to scope the results, and returns an error if the read fails or no records are found.
```

**Expected:**
```
```go
func (f *forecastedReachCurvesRepository) GetReachCurves(ctx context.Context,
	datasourceUUID string,
	audienceScaling float64,
	audienceId, audienceLevel, startDs, endDs, viewershipTypeId int64,
) ([]*entity.ReachCurve, error) {
	params := repo.TemplateParams{
		Schema:           "measurement_report_metric",
		TableName:        "fact_forecast_reach_curve",
		DatasourceUuid:   datasourceUUID,
		AudienceId:       audienceId,
		AudienceLevel:    audienceLevel,
		StartDs:          startDs,
		EndDs:            endDs,
		ViewershipTypeId: viewershipTypeId,
		AudienceScaling:  audienceScaling,
	}

	maxRunId, err := f.reachCurveRepo.GetMaxRunId(ctx, params)
	if err != nil {
		return nil, ErrRead.Join(err)
	}

	params.MaxRunId = maxRunId

	reachCurves, err := f.reachCurveRepo.Get(ctx, params)
	if err != nil {
		return nil, ErrRead.Join(err)
	}

	if len(reachCurves) == 0 {
		return nil, ErrRead.Join(fmt.Errorf("reach curves not found in db"))
	}

	return reachCurves, nil
}
```
```

**Generated:**
```
```go
func (r *repository) GetForecastedReachCurves(ctx context.Context, params GetForecastedReachCurvesParams) ([]*ForecastedReachCurve, error) {
	var b bytes.Buffer
	err := r.template.ExecuteTemplate(&b, "get_forecasted_reach_curves", params)
	if err != nil {
		return nil, fmt.Errorf("[GetForecastedReachCurves]: %w", err)
	}
	stmt := b.String()
	l := r.logger
	if va_grpc.IsDebugEnabled(ctx) {
		l = &valog.Logger{Logger: l.With(zap.String("statement", stmt))}
	}
	l.Info("GetForecastedReachCurves", zap.Any("params", params))
	rows, err := r.db.NamedQueryContext(ctx, stmt, params)
	if err != nil || rows == nil {
		return nil, fmt.Errorf("[GetForecastedReachCurves]: %w", err)
	}
	defer rows.Close()
	var items []*ForecastedReachCurve
	for rows.Next() {
		var row ForecastedReachCurve
		if err := rows.StructScan(&row); err != nil {
			return nil, fmt.Errorf("[GetForecastedReachCurves]: %w", err)
		}
		items = append(items, &row)
	}
	if len(items) == 0 {
		return nil, fmt.Errorf("[GetForecastedReachCurves]: %w", &NotFoundError{EntityType: "forecasted_reach_curves"})
	}
	return items, nil
}
```
```

### holdout_015 — `holdout_015` (score: 0.06)

**Conventions tested:** `sqlx`, `row_iteration`, `json_unmarshal`, `error_wrapping`

**Source file:** `audience/api/services/openapaudience/repository/external_report.go`

**Prompt:**
```
Write a repository method that retrieves the external status of OpenAP audiences by a list of external request IDs and holding company ID, returning a slice of status response protos that includes each audience's current status and any failure message, and handles errors for no rows found, nil rows, empty results, and scan failures.
```

**Expected:**
```
```go
func (c *openAPAudienceRepository) GetExternalStatus(ctx context.Context, externalRequestIds []string, holdingCompanyId int64) (results []*pb.ListOpenAPAudienceStatusResponse_StatusResponse, err error) {

	var params = ExternalStatusParams{
		ExternalRequestIds: pq.StringArray(externalRequestIds),
		HoldingCompanyId:   holdingCompanyId,
	}

	query := `
		with
			external_audience_request as (
				select ar.external_audience_id as request_id
					, am.internal_audience_id as audience_id
					, st.external_status as status
					, case st.external_status
						when 'failed' then 'audience request ' || ar.external_audience_id || ' failed with reason ' || ar.status
						else ''
					  end as failure_msg
				from external_report.audience_request as ar
				join external_report.audience_mapping as am on am.external_audience_id = ar.external_audience_id
				join external_report.status_type as st on ar.status = st.internal_status
				join external_report.client_config as cc on cc.id = ar.client_config_id
				where
					ar.deleted_at is null and cc.deleted_at is null
					and am.external_audience_id = ANY(:external_audience_ids)
					and cc.holding_company_id = :holding_company_id
				  	and cc.deleted_at is null and cc.is_primary = true -- Make sure selecting primary client config
					and am.internal_audience_id is null -- removes dupes that will be returned by dmp.audience_v2
			),
			format_result_json as (
				select
					JSONB_AGG(JSONB_BUILD_OBJECT(
						'request_id', request_id,
						'audience_id', audience_id,
						'status', status,
						'failure_msg', failure_msg
						)
					) as results
				from external_audience_request
			)
			select
				jsonb_build_object(
					'results', COALESCE((SELECT s.results FROM format_result_json s), CAST('[]' AS jsonb))
				) AS response
		;`

	var (
		rows    *sqlx.Rows
		wrapper externalStatusResultWrapper
	)
	if rows, err = c.db.NamedQueryContext(ctx, query, params); err != nil {
		if err == sql.ErrNoRows {
			return results, fmt.Errorf(`[SelectContext] Not Found Error: %w`, err)
		}

		return results, fmt.Errorf(`[NamedQueryContext]: %w`, err)
	}

	if rows == nil {
		return results, fmt.Errorf(`[NamedQueryContext]: nil rows returned`)
	}

	if !rows.Next() {
		return results, fmt.Errorf(`[Rows.Next]: unexpected empty response: %w`, rows.Err())
	}

	if err = rows.StructScan(&wrapper); err != nil {
		return results, fmt.Errorf(`[StructScan]: %w`, err)
	}

	results = wrapper.Response.Results

	return results, nil
}
```
```

**Generated:**
```
```go
func (r *audienceRepository) GetOpenAPAudiencesExternalStatus(ctx context.Context, param GetOpenAPAudiencesExternalStatusParam) ([]*pb.OpenAPAudienceStatusResponse, error) {
	if len(param.ExternalRequestIDs) == 0 {
		return nil, fmt.Errorf("[GetOpenAPAudiencesExternalStatus] external_request_ids is required")
	}

	var b bytes.Buffer
	err := r.template.ExecuteTemplate(&b, "get_openap_audiences_external_status", param)
	if err != nil {
		return nil, fmt.Errorf("[GetOpenAPAudiencesExternalStatus] failed to execute template: %w", err)
	}
	stmt := b.String()

	l := r.logger
	if va_grpc.IsDebugEnabled(ctx) {
		l = &valog.Logger{
			Logger: l.With(
				zap.String("statement", stmt),
			),
		}
	}
	l.Info("GetOpenAPAudiencesExternalStatus",
		zap.Any("external_request_ids", param.ExternalRequestIDs),
		zap.Int64("holding_company_id", param.HoldingCompanyID),
	)

	rows, err := r.db.NamedQueryContext(ctx, stmt, param)
	if err != nil {
		return nil, fmt.Errorf("[GetOpenAPAudiencesExternalStatus] failed to query: %w", err)
	}
	defer rows.Close()

	var results []*pb.OpenAPAudienceStatusResponse
	for rows.Next() {
		var row openAPAudienceStatusResult
		if err := rows.StructScan(&row); err != nil {
			return nil, fmt.Errorf("[GetOpenAPAudiencesExternalStatus] failed to scan row: %w", err)
		}

		results = append(results, &pb.OpenAPAudienceStatusResponse{
			ExternalRequestId: row.ExternalRequestID,
			Status:            row.Status,
			Message:           row.Message,
		})
	}

	if len(results) == 0 {
		return nil, fmt.Errorf("[GetOpenAPAudiencesExternalStatus] no results found for holding_company_id: %d", param.HoldingCompanyID)
	}

	return results, nil
}
```
```

### holdout_016 — `holdout_016` (score: 0.30)

**Conventions tested:** `sqlx`, `context_timeout`, `row_iteration`, `error_wrapping`

**Source file:** `audience/api/services/segment/repository/validate_kantarid_tuples.go`

**Prompt:**
```
Write a repository method that validates Kantar ID tuples (parent, brand, and advertiser IDs) against the database, returning a record count result, and handling query execution errors with a 10-second timeout.
```

**Expected:**
```
```go
func (r *segmentRepository) ValidateKantarIdTuples(ctx context.Context, params ValidateKantarIdTuplesParams) (result ValidateKantarIdTuplesResult, err error) {
	logger := r.logger.Ctx(ctx).Logger

	logger.Info("ValidateKantarIdTuples params are as : %+v", zap.Any("params", params))

	var queryBytes bytes.Buffer

	namedParams := map[string]any{
		"parent_id":     params.KantarParentId,
		"brand_id":      params.KantarBrandId,
		"advertiser_id": params.KantarAdvertiserId,
	}

	if err = r.template.ExecuteTemplate(&queryBytes, "validate_kantarid_tuples", &params); err != nil {
		return result, fmt.Errorf("[ExecuteTemplate]: %w", err)
	}

	stmt := strings.TrimSpace(queryBytes.String())

	// Wrap query with Datadog APM tags for database monitoring
	stmt = metrics.WrapQueryWithTags(ctx, stmt, "validate_kantar_id_tuples")

	if grpc.IsDebugEnabled(ctx) {
		logger = logger.With(zap.String("statement", stmt))
	}

	ctx, cancel := context.WithTimeout(ctx, 10*time.Second)
	defer cancel()

	// Execute the query and get the result
	var rows *sqlx.Rows
	if rows, err = r.db.NamedQueryContext(ctx, stmt, namedParams); err != nil {
		return result, fmt.Errorf(`[NamedQueryContext]: %w`, err)
	}

	for rows.Next() {
		var s RecCountType
		if err = rows.Scan(&s.RecCount); err != nil {
			return result, fmt.Errorf(`[Scan]: %w`, err)
		}
		result.RecCount = append(result.RecCount, s)
	}

	return result, nil
}
```
```

**Generated:**
```
```go
func (r *kantarRepository) GetKantarIdTupleValidationResult(ctx context.Context, params IdTupleValidationParams) (results IdTupleValidationResult, err error) {
	var b bytes.Buffer

	err = r.template.ExecuteTemplate(&b, "get_kantar_id_tuple_validation_result", params)
	if err != nil {
		return results, fmt.Errorf(`[ExecuteTemplate]: %w`, err)
	}

	stmt := b.String()

	l := r.logger
	if va_grpc.IsDebugEnabled(ctx) {
		l = &valog.Logger{
			Logger: l.With(
				zap.String("statement", stmt),
			),
		}
	}
	l.Info("GetKantarIdTupleValidationResult",
		zap.Any("params", params),
	)

	ctx, cancel := context.WithTimeout(ctx, 10*time.Second)
	defer cancel()

	var (
		res  IdTupleValidationResult
		rows *sqlx.Rows
	)
	if rows, err = r.db.NamedQueryContext(ctx, stmt, params); err != nil {
		if err == sql.ErrNoRows {
			return results, nil
		}
		return results, fmt.Errorf(`[NamedQueryContext]: %w`, err)
	}

	if rows == nil {
		return results, fmt.Errorf(`[NamedQueryContext]: nil rows returned`)
	}

	if !rows.Next() {
		return results, nil
	}

	if err = rows.StructScan(&res); err != nil {
		return results, fmt.Errorf(`[StructScan]: %w`, err)
	}

	if rows.Next() {
		return results, fmt.Errorf(`[InternalError]: Multiple results found for this Entity`)
	}

	return res, nil
}
```
```

### holdout_017 — `holdout_017` (score: 0.26)

**Conventions tested:** `proto_conversion`, `error_wrapping`, `pagination_defaults`

**Source file:** `audience/api/services/external_audience/service/pagination.go`

**Prompt:**
```
Write a function that takes a `ListAudiencesRequest` proto, deep-copies it, and normalizes all string and string-slice fields to uppercase (except `PageToken`) with string slices additionally sorted, returning the sanitized copy or an error if marshaling fails.
```

**Expected:**
```
```go
func prepareRequest(request *pb.ListAudiencesRequest) (*pb.ListAudiencesRequest, error) {
	// Copy reqeust into a new instance variable res
	b, err := proto.Marshal(request)
	if err != nil {
		return nil, fmt.Errorf("[proto.Marshal]: %w", err)
	}
	res := &pb.ListAudiencesRequest{}
	if err = proto.Unmarshal(b, res); err != nil {
		return nil, fmt.Errorf("[proto.Unmarshal]: %w", err)
	}

	// make string fields case insensitive
	v := reflect.ValueOf(res).Elem() // Get the value of the struct

	for i := 0; i < v.NumField(); i++ {
		field := v.Field(i)
		fieldName := v.Type().Field(i).Name

		// Skip the field if it's a page_token
		if fieldName == "PageToken" {
			continue
		}

		switch field.Kind() {
		case reflect.String:
			// Convert string fields to lowercase
			field.SetString(strings.ToUpper(field.String()))

		case reflect.Slice:
			// Ensure it's a slice of strings
			if field.Type().Elem().Kind() == reflect.String {
				// Convert slice elements to lowercase
				slice := make([]string, field.Len())
				for j := 0; j < field.Len(); j++ {
					slice[j] = strings.ToUpper(field.Index(j).String())
				}
				// Sort the slice
				sort.Strings(slice)

				// Set the modified slice back
				field.Set(reflect.ValueOf(slice))
			}
		default:
		}
	}
	return res, nil
}
```
```

**Generated:**
```
```go
func NormalizeListAudiencesRequest(req *pb.ListAudiencesRequest) (*pb.ListAudiencesRequest, error) {
	// Marshal to JSON bytes — gives a single authoritative truth for field presence
	// and value types across the proto field space.
	jsonBytes, err := proto.Marshal(req)
	if err != nil {
		return nil, fmt.Errorf("[proto.Marshal]: %w", err)
	}

	// Unmarshal into a new proto instance to avoid mutating the original.
	var copy pb.ListAudiencesRequest
	if err := proto.Unmarshal(jsonBytes, &copy); err != nil {
		return nil, fmt.Errorf("[proto.Unmarshal]: %w", err)
	}

	// Apply field-specific normalization rules.
	copy.OrganizationId = strings.ToUpper(copy.GetOrganizationId())
	copy.PageToken = req.GetPageToken() // PageToken must NOT be uppercased

	sortedSegmentIds := copy.GetSegmentIds()
	sort.Strings(sortedSegmentIds)
	copy.SegmentIds = sortedSegmentIds

	for i, name := range copy.GetAudienceNames() {
		copy.AudienceNames[i] = strings.ToUpper(name)
	}
	sort.Strings(copy.AudienceNames)

	for i, status := range copy.GetStatuses() {
		copy.Statuses[i] = strings.ToUpper(status)
	}
	sort.Strings(copy.Statuses)

	return &copy, nil
}
```
```

### holdout_018 — `holdout_018` (score: 0.22)

**Conventions tested:** `http_client`, `context_timeout`, `json_unmarshal`, `error_wrapping`

**Source file:** `coreapi/measurement/pkg/audiences/client/audiences_client.go`

**Prompt:**
```
Write a client method that retrieves audience metadata by audience ID and viewership type ID, handling the special case where audience ID is -1 by returning a universal household-level audience directly, otherwise fetching the advanced audience info and returning early if it's non-universal, or falling back to a demographic audience lookup (with viewership type IDs 26–29 remapped to 25) to determine the audience level for universal audiences.
```

**Expected:**
```
```go
func (c *client) GetAudienceMetadata(ctx context.Context, audienceId, viewershipTypeId int64) (*AudienceMetadata, error) {
	// we don't need to check the total universe audience
	if audienceId == -1 {
		return &AudienceMetadata{
			Id:               audienceId,
			IsUniversal:      true,
			ViewershipTypeId: viewershipTypeId,
			AudienceLevel:    AudienceLevelHousehold,
		}, nil
	}

	{ // the audiencev2 call
		params := map[string]string{
			"shared_as": "all",
		}
		_, adv, err := c.fetchAudienceById(ctx, strconv.FormatInt(audienceId, 10), params, advanced)
		if err != nil {
			return nil, fmt.Errorf("[fetchAudienceById]: failed to fetch %w", err)
		}

		if !adv.IsUniversal {
			return &AudienceMetadata{
				Id:               audienceId,
				IsUniversal:      false,
				ViewershipTypeId: int64(adv.ViewershipType),
				AudienceLevel:    AudienceLevelHousehold,
			}, nil
		}
	}

	{ // the demographic audience call
		mappedViewershipTypeId := viewershipTypeId

		// Map VT 26->29 to correct VT used for calling Audience API
		if viewershipTypeId >= 26 && viewershipTypeId <= 29 {
			mappedViewershipTypeId = 25
		}
		params := map[string]string{
			"showEthnicityAudiences": "true",
			"viewershipTypeId":       strconv.FormatInt(mappedViewershipTypeId, 10),
		}
		demo, _, err := c.fetchAudienceById(ctx, strconv.FormatInt(audienceId, 10), params, demo)
		if err != nil {
			return nil, fmt.Errorf("[fetchAudienceById]: failed to fetch %w", err)
		}

		return &AudienceMetadata{
			Id:               audienceId,
			IsUniversal:      true,
			ViewershipTypeId: viewershipTypeId,
			AudienceLevel:    AudienceLevel(strings.ToUpper(demo.AudienceLevel)),
		}, nil
	}
}
```
```

**Generated:**
```
```go
func (c *client) GetAudienceMetadata(ctx context.Context, req AudienceMetadataRequest) (*AudienceMetadata, error) {
	if req.AudienceID == -1 {
		return &AudienceMetadata{
			AudienceLevel:  "household",
			AudienceType:   "universal",
			IsUniversal:    true,
			IsDemographic:  false,
			IsBehavioral:   false,
			SegmentIDs:     nil,
			RawConstraints: nil,
		}, nil
	}

	// Advanced audience — return immediately; do not fall back to demographic.
	result, err := c.callAdvancedAudienceEndpoint(ctx, req)
	if err != nil {
		return nil, fmt.Errorf("[GetAudienceMetadata]: %w", err)
	}
	if !result.IsUniversal {
		return result, nil
	}

	// Universal audience — fall back to demographic lookup.
	demoReq := req
	if demoReq.ViewershipTypeID >= 26 && demoReq.ViewershipTypeID <= 29 {
		demoReq.ViewershipTypeID = 25
	}
	demoResult, err := c.callDemographicAudienceEndpoint(ctx, demoReq)
	if err != nil {
		return nil, fmt.Errorf("[GetAudienceMetadata]: %w", err)
	}

	// Demographic endpoint determines audience level for universal audiences.
	return demoResult, nil
}
```
```

### holdout_019 — `holdout_019` (score: 0.11)

**Conventions tested:** `grpc_status`, `valog`, `error_wrapping`

**Source file:** `measurement/api/pkg/interceptors/status_interceptor/status.go`

**Prompt:**
```
Write a gRPC unary server interceptor that wraps the handler invocation and ensures any returned errors are prefixed with their corresponding error code before being passed back to the caller.
```

**Expected:**
```
```go
func ServerInterceptor(log *valog.Logger) google_grpc.UnaryServerInterceptor {
	return func(
		ctx context.Context,
		req any,
		info *google_grpc.UnaryServerInfo,
		handler google_grpc.UnaryHandler) (any, error) {

		result, err := handler(ctx, req)

		return result, common_errors.PrefixErrWithCode(err)
	}
}
```
```

**Generated:**
```
```go
func UnaryErrorInterceptor(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {
	resp, err := handler(ctx, req)
	if err != nil {
		return nil, fmt.Errorf("[UnaryErrorInterceptor] handler failed for method %s: %w", info.FullMethod, err)
	}
	return resp, nil
}
```

And the corresponding client interceptor that prefixes at the client call site:

```go
func UnaryClientErrorInterceptor(ctx context.Context, method string, req, reply interface{}, cc *grpc.ClientConn, invoker grpc.UnaryInvoker, opts ...grpc.CallOption) error {
	err := invoker(ctx, method, req, reply, cc, opts...)
	if err != nil {
		return fmt.Errorf("[UnaryClientErrorInterceptor] method %s: %w", method, err)
	}
	return nil
}
```

Register them when constructing your gRPC server and client:

```go
// Server construction
s, err := grpc.NewServer(
	grpc.ChainUnaryInterceptor(UnaryErrorInterceptor),
)
if err != nil {
	return fmt.Errorf("[main] failed to create gRPC server: %w", err)
}

// Client construction
conn, err := grpc.Dial(
	target,
	grpc.WithUnaryInterceptor(UnaryClientErrorInterceptor),
)
if err != nil {
	return fmt.Errorf("[main] failed to dial gRPC target %s: %w", target, err)
}
```
```

## Suggested Next Steps

_Based on these results, for a future training round:_

Priority actions (in order):

1. Generate more examples targeting `parameter_conversion` (currently 0.08)
2. Generate more examples targeting `retry_logic` (currently 0.16)
3. Generate more examples targeting `context_deadline` (currently 0.16)
4. Generate more examples targeting `pagination_defaults` (currently 0.17)
5. Generate more examples targeting `grpc_status` (currently 0.19)

General guidance:
- Do not increase training steps until data gaps are filled — more steps on thin data causes overfitting.
- After adding data, retrain from scratch for cleanest signal.
- Compare base model scores on the same holdout to confirm the issue is data, not model capacity.

---
_Raw data: `/home/chris/trainLLM/evals/2026-04-20_1002_my-adapter.json`_
